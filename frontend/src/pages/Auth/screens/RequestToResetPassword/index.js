import React from 'react';
import PropTypes from 'prop-types';

RequestToResetPassword.propTypes = {
    
};

function RequestToResetPassword(props) {
    return (
        <div>
            
        </div>
    );
}

export default RequestToResetPassword;